name: Test

on:
    push:

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: marshall-ku/workflows/.github/actions/setup_pnpm.yml@master
            - name: Run script
              run: pnpm start
